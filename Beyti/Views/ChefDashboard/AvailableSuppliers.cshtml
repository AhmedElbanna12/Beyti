@model IEnumerable<Beyti.ViewModel.chief.SupplyVM>
@{
    ViewData["Title"] = "Available Suppliers";
}

<div class="container mt-4">
    <div class="suppliers-header">
        <h2>📦 Available Suppliers</h2>
        <p style="font-size: 1.1rem; opacity: 0.9; margin-top: 10px;">Find quality ingredients for your recipes</p>
    </div>

    @if (Model?.Any() == true)
    {
        <div class="suppliers-table-container">
            <table class="suppliers-table">
                <thead>
                    <tr>
                        <th>Company Name</th>
                        <th>Category</th>
                        <th>Address</th>
                        <th>Contact</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var supplier in Model)
                    {
                        <tr>
                            <td data-label="Company" class="supplier-company">
                                🏢 @supplier.SupplierName
                            </td>
                            <td data-label="Category">
                                <span class="supplier-category">@supplier.Category</span>
                            </td>
                            <td data-label="Address" class="supplier-contact">
                                <i class="bi bi-geo-alt"></i> @supplier.Address
                            </td>
                            <td data-label="Phone" class="supplier-contact">
                                <i class="bi bi-telephone"></i> @supplier.PhoneNumber
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="suppliers-table-container">
            <div class="empty-suppliers">
                <i class="bi bi-box-seam"></i>
                <h3>No Suppliers Available</h3>
                <p>Check back later for new suppliers</p>
            </div>
        </div>
    }

    <a asp-action="Dashboard" class="back-to-dashboard-btn">
        ← Back to Dashboard
    </a>
</div>
